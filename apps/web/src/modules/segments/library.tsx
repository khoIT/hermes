/**
 * 03 — Segments Library (seg_library)
 * Placeholder — wired in Phase 7.
 */
import React from 'react';
import { T } from '../../theme';

export default function SegmentsLibraryPage() {
  return (
    <div style={{ padding: '32px 40px' }}>
      <div style={{ fontFamily: T.fSans, fontSize: 10, fontWeight: 600, color: T.n400, textTransform: 'uppercase', letterSpacing: '0.1em', marginBottom: 6 }}>03 · Segments</div>
      <div style={{ fontFamily: T.fDisp, fontSize: 40, textTransform: 'uppercase', color: T.n950, lineHeight: 0.95, marginBottom: 16 }}>Segments</div>
      <p style={{ fontFamily: T.fSans, fontSize: 13, color: T.n500 }}>Screen 03 · seg_library · Phase 7 implementation pending.</p>
    </div>
  );
}
